services:
  bot:
    image: fernuni-bot:latest
    container_name: fernuni-bot
    user: "${UID}:${GID}"
    env_file: ${HOME}/.env
    volumes:
      - ${HOME}/data:/app/data
    restart: unless-stopped
