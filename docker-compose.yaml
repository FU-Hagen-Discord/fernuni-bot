services:
  bot:
    image: fernuni-bot:latest
    container_name: fernuni-bot
    user: "${USERID}:${GROUPID}"
    env_file: ${HOME}/.env
    volumes:
      - ${HOME}/data:/app/data
    restart: unless-stopped
